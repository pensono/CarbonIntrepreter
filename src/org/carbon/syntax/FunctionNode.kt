package org.carbon.syntax

import org.carbon.runtime.CarbonExpression
import org.carbon.runtime.CarbonScope
//import org.carbon.runtime.ScalarExpression

/**
 * @author Ethan
 */
// Eventually should also include parameter types
//class FunctionNode(private val parameterNames: List<String>,
//                   private val body: Node) : Node() {
//
//    override fun link(scope: CarbonScope): CarbonExpression {
//        return ScalarExpression(scope, body, parameterNames)
//    }
//
//    override fun getShortString(): String = "Function Node. Parameter Names: " + parameterNames.joinToString(", ") + ". Body:"
//    override fun getBodyString(level: Int): String = body.getFullString(level + 1)
//}
